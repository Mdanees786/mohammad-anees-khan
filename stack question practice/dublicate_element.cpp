#include<iostream>
#include<stack>
using namespace std;
int main(){
    stack<int>st;
    stack<int>st2;
    st.push(1);
    st.push(2);
    st.push(3);
    st.push(4);
    st.push(3);
    int n= st.size();
    while(!st.empty()){
        if()
    }
}